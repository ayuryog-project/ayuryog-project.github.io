source "https://rubygems.org"

gem "jekyll", "~> 4.2"
gem "jekyll-seo-tag", "~> 2.8"
gem "webrick"              # needed for Ruby 3+

# Pin sass-converter to 2.x, which uses pure-Ruby sassc
# instead of sass-embedded (which requires native extensions / rake)
gem "jekyll-sass-converter", "~> 2.0"

group :jekyll_plugins do
  gem "jekyll-seo-tag", "~> 2.8"
end
